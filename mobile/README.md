# Vintrace Mobile

This is a React Native application bootstrapped with Expo.

## Getting Started

1.  **Install dependencies:**

    ```bash
    cd mobile && npm install
    ```

2.  **Run the application:**

    -   To run on Android:

        ```bash
        npm run android
        ```

    -   To run on iOS:

        ```bash
        npm run ios
        ```
